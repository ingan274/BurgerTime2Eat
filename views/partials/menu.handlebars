<p class = "burgers">
    {{#if devoured}}
    <button class="eatButton" id="tryBurger" data-id="{{burger_id}}" data-eaten="{{devoured}}">
        Devour
    </button>
    {{else}}
    {{/if}}
    {{burger_name}}
</p>